#ifndef READFILE_H__
#define READFILE_H__

#include <string>
#include <iostream>
#include <fstream>

using namespace std;

// TODO add detailed explanation on what the function should do
// addToArrayDesc should add up to 100 floats into an array in decending order and peint thr array after every element added
int addToArrayDesc(float sortedArray[], int numElements, float newValue);

#endif // READFILE_H__